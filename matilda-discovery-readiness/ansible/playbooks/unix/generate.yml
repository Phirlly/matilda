---
- name: Generate UNIX readiness instructions scaffold
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Run UNIX readiness scaffold
      ansible.builtin.import_role:
        name: matilda_unix_target
