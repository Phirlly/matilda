---
- name: Windows preflight scaffold
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Run Windows readiness scaffold
      ansible.builtin.import_role:
        name: matilda_windows_target
